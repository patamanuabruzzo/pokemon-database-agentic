# Start your applications

## Instructions

- Run the commands in the `Run` section below top to bottom.
- For development: Use `npm run dev` to start the Next.js development server with hot reload
- For production: Use `npm run build && npm run start` to build and start the production server

## Run
npm run dev